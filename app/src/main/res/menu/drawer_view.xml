<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">
    <group android:checkableBehavior="single">
        <item
                android:id="@+id/mail"
                android:title="mail"
                android:icon="@drawable/mail48dp"/>
        <item
                android:id="@+id/send"
                android:title="send"
                android:icon="@drawable/send48dp"/>
        <item
                android:id="@+id/camera"
                android:title="camera"
                android:icon="@drawable/camera48dp"/>
        <item
                android:id="@+id/info"
                android:title="info"
                android:icon="@drawable/info48dp"/>
    </group>
</menu>